//- Global Navbar
-
  menu = {
    'Home': '/',
    'Resume': '/resume'
  };
nav.navbar.navbar-inverse.navbar-fixed-top
  div.container
    div.navbar-header
      a.navbar-brand(href='/', style='font-size: 25px') Jake Portanova
    div.navbar-collapse.collapse
      ul.nav.navbar-nav.navbar-right
        each val, key in menu
          if selected === key
            li.active: a(href=val)= key
          else
            li: a(href=val)= key
